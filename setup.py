from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='its a wafer project to classify whether a wafer is functional +MLops integration',
    author='Sherin98',
    license='MIT',
)
